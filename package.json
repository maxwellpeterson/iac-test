{
  "name": "iac-test",
  "description": "Testing out deploying Cloudflare Workers with SST and Terraform.",
  "scripts": {
    "deploy:sst" : "npx sst deploy --stage production",
    "deploy:terraform" : "esbuild index.terraform.ts --outdir=build && terraform deploy -var cloudflare_api_token=$CLOUDFLARE_API_TOKEN"
  },
  "license": "ISC",
  "dependencies": {
    "sst": "3.8.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250204.0",
    "esbuild": "^0.25.0"
  }
}
